- content_for(:title) { "Compare JavaScript libraries by weight" }

.row
  .four.columns
    %aside.categories
      %h3 By Category
      %ul
        %li= link_to "All Libraries", root_path, data: { category: 'all' }
        = render @categories

  .eight.columns
    %section.libraries
      %h3 Libraries
      .sort
        %span Sort by:
        = link_to "Weight", url_for(sort: 'weight'), data: { sort: 'weight' }
        = link_to "Popularity", url_for(sort: 'popularity'), data: { sort: 'popularity' }
      %ol
      %p.none-found No libraries found.
      %noscript
        %p
          Enable JavaScript and reload to use this site. It is about JavaScript libraries after all.
